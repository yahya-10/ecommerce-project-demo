{
  "scripts": {
    "front": "cd client && npm start",
    "back": "cd server && npm run server",
    "dev": "concurrently \"npm run front\" \"npm run back\" "
  },
  "dependencies": {
    "concurrently": "^7.0.0"
  }
}
